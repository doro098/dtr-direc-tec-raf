{
  "name": "dtr-direc-tec-raf",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo padre: tag-mule + tureparto — AI-powered text enrichment and tagging system",
  "scripts": {
    "install": "git submodule update --init --recursive",
    "dev": "docker compose up -d --build",
    "preview": "docker compose up -d --build",
    "build": "docker compose build",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "logs:tag-mule": "docker compose logs -f tag-mule",
    "logs:cliente-tureparto": "docker compose logs -f cliente-tureparto",
    "pull-models": "docker compose exec -it ollama ollama pull qwen2.5:3b && docker compose exec -it ollama ollama pull nomic-embed-text",
    "health": "curl -s http://localhost:8080/api/v1/health | python3 -m json.tool"
  }
}
